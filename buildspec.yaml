version: 0.2
phases:
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket serverlessrepo-aws-sam-codepipeline-cd-artifacts-bzyyzr0in3d6 --output-template-file packaged-template.yaml
artifacts:
  files:
    - packaged-template.yaml
